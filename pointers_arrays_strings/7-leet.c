#include "main.h"

/**
 * leet -
 * @str:
 * Return:
 */

char *leet(char *str)
{
	while (*str != '\0')
	{
		if (*str == 'a' || *str == 'A' || *str == 'e' || *str == 'E' || )
		{
			_putchar('
		str++;
	}
}
